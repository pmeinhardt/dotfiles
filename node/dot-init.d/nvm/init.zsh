export NVM_DIR="$HOME/.nvm"

# Load nvm (defer version switching until `nvm use` is invoked)
[ -s "$NVM_DIR/nvm.sh" ] && . "$NVM_DIR/nvm.sh" --no-use

# Load nvm shell completion
# [ -s "$NVM_DIR/bash_completion" ] && . "$NVM_DIR/bash_completion"
