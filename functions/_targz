#compdef targz

emulate -L zsh

local context state state_descr line
_arguments \
  '1:source path:->source' \
  '2:archive name:'

if [ $? -ne 300 ]; then
  return 0
fi

case "$state" in
  source)
    _path_files
    compadd -Q . ..
    ;;
esac
